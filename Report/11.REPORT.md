# MagicSquare_xx STEP 11 — Dual Track REFACTOR (Logic·UI 분리)

| 항목 | 내용 |
|------|------|
| **프로젝트** | MagicSquare_xx / MagicSquare_1004 |
| **경로** | `c:\김정화\DEV\MagicSquare_xx` |
| **작성일** | 2026-06-11 |
| **단계** | STEP 11 — Dual Track TDD 리팩터 |
| **Phase** | refactor |
| **Test ID** | T-INC-01, T-FAIL-D1, T-FAIL-FAKE, T-PASS-01 (+ golden 4) |
| **Command** | 사용자 요청 Dual Track 리팩터 → `/export` |
| **참고** | [Report/10.REPORT.md](10.REPORT.md) |
| **Transcript** | [Prompting/11.Export-Transcript.md](../Prompting/11.Export-Transcript.md) |

---

## 1. 개요

`src/validate_lines.py`에 혼재하던 **Logic Track**(10선·셀 규칙)과 **UI Track**(격자 입출력·dict 계약·Golden 직렬화·표시 문자열)을 ECB 계층으로 분리했다. Control은 `validate_lines` 진입점만 유지하고 Logic·UI를 각 Track 모듈에 위임한다. pytest **8 passed**, golden **matched** — 계약·동작 불변.

| 항목 | 내용 |
|------|------|
| **ARRR** | Refine — Dual Track 구조 분리 |
| **Track** | Logic + UI · Layer: entity + boundary |
| **판정** | ✅ REFACTOR 완료 |

---

## 2. 실행 증거 (Step A 실측)

### 2.1 pytest

```bash
python -m pytest tests/ -v
```

| 항목 | 결과 |
|------|------|
| **명령** | `python -m pytest tests/ -v` |
| **결과** | **PASSED — 8 passed** (0.05s) |
| **Test ID** | T-INC-01, T-FAIL-D1, T-FAIL-FAKE, T-PASS-01 (+ golden 4) |

### 2.2 git status

| 항목 | 내용 |
|------|------|
| **브랜치** | (기본 브랜치) |
| **변경** | `M` README, demo/app.py, src/validate_lines.py, tests/_approval.py, tests/test_validate_lines.py · `??` boundary/, entity/line_logic.py 등 |
| **commit** | 없음 (Export만) |

---

## 3. Phase별 STEP

### 3.1 STEP: RED

해당 없음.

### 3.2 STEP: GREEN

해당 없음 (Report/08에서 완료).

### 3.3 STEP: REFACTOR

| 항목 | 내용 |
|------|------|
| **RF-xx** | Dual Track 분리 (Logic → entity, UI → boundary) |
| **Command** | 사용자 직접 요청 (smell/safe 체인 없이 일괄 적용) |
| **Budget** | 파일 9 · 클래스 0 · 메서드 10+ (사용자 명시 범위) |
| **pytest** | PASSED — 8 tests |
| **golden** | matched: yes (`UPDATE_GOLDEN` 없음) |

**Logic Track** (`entity/line_logic.py`):

- `LINE_IDS`, `has_blank_cells`, `has_invalid_values`, `has_duplicate_values`, `failed_lines`
- `entity`는 `boundary`·`control` import 없음

**UI Track** (`boundary/`):

- `grid_io.py` — `flatten_grid`, `build_validation_result`, `parse_grid_texts`, `format_grid_for_display`
- `golden_format.py` — `format_golden` (INT6·ERR 직렬화)
- `status_display.py` — 데모 status·failed_lines 표시 문자열

**Control** (`src/validate_lines.py`):

- Logic·UI Track 조율만 — 도메인·I/O 로직 없음

### 3.4 STEP: repeat

해당 없음.

---

## 4. 변경 파일

| 경로 | Phase | 요약 |
|------|-------|------|
| `entity/line_logic.py` | REFACTOR | Logic Track — 10선·셀 도메인 규칙 |
| `boundary/__init__.py` | REFACTOR | boundary 패키지 |
| `boundary/grid_io.py` | REFACTOR | UI Track — 격자·dict 계약 |
| `boundary/golden_format.py` | REFACTOR | UI Track — Golden 직렬화 |
| `boundary/status_display.py` | REFACTOR | UI Track — status 표시 문자열 |
| `src/validate_lines.py` | REFACTOR | Control — thin orchestration |
| `tests/_approval.py` | REFACTOR | `format_golden` → boundary 위임 |
| `tests/test_validate_lines.py` | REFACTOR | golden import 경로 정리 |
| `demo/app.py` | REFACTOR | Boundary 모듈 사용 |
| `Report/11.REPORT.md` | Export | 본 보고서 |
| `Prompting/11.Export-Transcript.md` | Export | Transcript |

---

## 5. 산출물·폴더

```
c:\김정화\DEV\MagicSquare_xx\
├── entity/
│   ├── constants.py
│   └── line_logic.py          # Logic Track
├── boundary/
│   ├── grid_io.py             # UI Track — 격자·dict
│   ├── golden_format.py       # UI Track — Golden
│   └── status_display.py      # UI Track — 표시
├── src/
│   └── validate_lines.py      # Control
├── demo/
│   └── app.py                 # Boundary 데모 (리팩터)
├── Report/
│   └── 11.REPORT.md           # 본 보고서
└── Prompting/
    └── 11.Export-Transcript.md
```

---

## 6. 판정·다음

| 항목 | 판정 |
|------|------|
| Dual Track 분리 | ✅ |
| pytest PASSED (8) | ✅ |
| golden matched | ✅ |
| **다음** | `/refactor-smell` 추가 스멜 점검 · Solver · GridUI · ECB (후속) |
